# PyInstaller spec for building the Windows executable.
# Build with: pyinstaller packaging/windows/PhotoToPDF.spec
from pathlib import Path

block_cipher = None
app_py = str(Path(SPECPATH).resolve().parent.parent / "src" / "app.py")

a = Analysis(
    [app_py],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name="Photo to PDF",
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
